# Category VAT policy

Catalog categories are based on current billing-report products. The `ירקות` category is VAT-exempt (0%) per the application's configured business rule. Other categories use the configured standard VAT rate unless explicitly marked exempt.
