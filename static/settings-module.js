(()=>{'use strict';const $=id=>document.getElementById(id);const setText=(id,v)=>{const e=$(id);if(e)e.textContent=v};const setHTML=(id,v)=>{const e=$(id);if(e)e.innerHTML=v};const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));async function api(url,opt={}){opt.credentials='same-origin';opt.headers=opt.headers||{};if(['POST','PUT','DELETE'].includes((opt.method||'GET').toUpperCase()))opt.headers['X-Requested-With']='XMLHttpRequest';try{const r=await fetch(url,opt);if(r.status===401){location.href='/login';return null}return r}catch(e){return null}}async function init(){const role=document.body.dataset.role||'viewer';const budget=$('budget-setting');if(budget){budget.value=localStorage.getItem('monthly_budget')||'';budget.onchange=()=>localStorage.setItem('monthly_budget',budget.value||'')}const theme=$('settings-theme');theme?.addEventListener('click',()=>{const dark=document.documentElement.classList.toggle('dark');localStorage.setItem('theme',dark?'dark':'light')});$('backup-btn')?.addEventListener('click',async()=>{const r=await api('/api/backup');if(!r||!r.ok){window.saasToast?.('הגיבוי נכשל');return}const b=new Blob([JSON.stringify(await r.json(),null,2)],{type:'application/json'});const a=document.createElement('a');a.href=URL.createObjectURL(b);a.download='smart-pricing-backup-'+new Date().toISOString().slice(0,10)+'.json';a.click();URL.revokeObjectURL(a.href)});try{const r=await fetch('/health');setText('health-status',r.ok?'🟢 תקין':'🔴 תקלה')}catch(e){setText('health-status','🔴 תקלה')}if(role==='admin'){const danger=$('danger-zone');if(danger)danger.style.display='block';$('season-reset-btn')?.addEventListener('click',resetSeason);await Promise.all([loadUsers(),loadLogs()])}else setHTML('users-area','<div class="saas-empty">ניהול משתמשים ויומן זמינים למנהל בלבד.</div>')}async function loadUsers(){const r=await api('/api/users');if(!r||!r.ok)return;const u=await r.json();setHTML('users-area',`<table class="saas-table"><thead><tr><th>משתמש</th><th>הרשאה</th><th>פעולה</th></tr></thead><tbody>${u.map(x=>`<tr><td><strong>${esc(x.username)}</strong></td><td>${x.role==='admin'?'מנהל':x.role==='editor'?'עורך':'צפייה'}</td><td><button class="saas-button danger" data-del-user="${x.id}">מחיקה</button></td></tr>`).join('')}</tbody></table>`);const area=$('users-area');area?.querySelectorAll('[data-del-user]').forEach(b=>b.onclick=async()=>{if(!confirm('למחוק משתמש?'))return;const r=await api('/api/users/'+b.dataset.delUser,{method:'DELETE'});if(r&&r.ok)loadUsers()})}async function loadLogs(){const r=await api('/api/logs');if(!r||!r.ok)return;const x=await r.json();setHTML('logs-area',x.map(l=>`<tr><td>${esc(l.time)}</td><td>${esc(l.user)}</td><td>${esc(l.action)}</td><td>${esc(l.details)}</td></tr>`).join('')||'<tr><td colspan="4" class="saas-empty">אין פעילות</td></tr>')}async function resetSeason(){if(!confirm('לאפס את העונה?'))return;const r=await api('/api/bulk/season',{method:'DELETE'});window.saasToast?.(r&&r.ok?'העונה אופסה':'הפעולה נכשלה')}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init()})();