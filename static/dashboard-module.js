(()=>{'use strict';let chart=null;const $=id=>document.getElementById(id);function iso(d){d.setMinutes(d.getMinutes()-d.getTimezoneOffset());return d.toISOString().slice(0,10)}function money(v){return Number(v||0).toLocaleString('he-IL',{minimumFractionDigits:2,maximumFractionDigits:2})}async function load(){const a=$('dash-from').value,b=$('dash-to').value;if(!a||!b)return;const r=await fetch(`/api/dashboard/summary?from=${encodeURIComponent(a)}&to=${encodeURIComponent(b)}`);if(!r.ok)return;const d=await r.json();$('dash-total').textContent=money(d.total||0)+' ₪';$('dash-regular').textContent=money(d.regular_total||0)+' ₪';$('dash-extra').textContent=money(d.extra_total||0)+' ₪';$('dash-days').textContent=d.days_count||0;const products=d.products||d.product_summary||[];$('dash-products').innerHTML=products.map(x=>`<tr><td><strong>${String(x.product_name||x.name||'')}</strong></td><td>${x.quantity||0}</td><td>${money(x.total||x.amount||0)} ₪</td></tr>`).join('')||'<tr><td colspan="3" class="saas-empty">אין נתונים</td></tr>';const days=d.days||d.daily||[];if(chart)chart.destroy();chart=new Chart($('dash-chart'),{type:'line',data:{labels:days.map(x=>x.date),datasets:[{label:'סה״כ',data:days.map(x=>x.total||0),tension:.3}]},options:{responsive:true,maintainAspectRatio:false}})}function init(){const t=new Date();$('dash-to').value=iso(new Date(t));const s=new Date(t);s.setDate(s.getDate()-30);$('dash-from').value=iso(s);$('dash-load').onclick=load;load()}init()})();