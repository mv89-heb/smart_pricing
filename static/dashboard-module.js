(()=>{'use strict';let chart=null;const $=id=>document.getElementById(id);const setText=(id,v)=>{const e=$(id);if(e)e.textContent=v};const setHTML=(id,v)=>{const e=$(id);if(e)e.innerHTML=v};function iso(d){d.setMinutes(d.getMinutes()-d.getTimezoneOffset());return d.toISOString().slice(0,10)}function money(v){return Number(v||0).toLocaleString('he-IL',{minimumFractionDigits:2,maximumFractionDigits:2})}async function load(){const from=$('dash-from'),to=$('dash-to');if(!from||!to)return;const a=from.value,b=to.value;if(!a||!b)return;const r=await fetch(`/api/dashboard/summary?from=${encodeURIComponent(a)}&to=${encodeURIComponent(b)}`);if(!r.ok)return;const d=await r.json(),s=d.summary||{};setText('dash-total',money(s.grand_total)+' ₪');setText('dash-regular',money(s.regular_total)+' ₪');setText('dash-extra',money(s.extra_total)+' ₪');setText('dash-days',s.days_count||0);const products=d.product_summary||{};setHTML('dash-products',Object.entries(products).sort((a,b)=>b[1].total-a[1].total).map(([name,x])=>`<tr><td><strong>${String(name)}</strong></td><td>${x.quantity||0}</td><td>${money(x.total)} ₪</td></tr>`).join('')||'<tr><td colspan="3" class="saas-empty">אין נתונים</td></tr>');const days=d.day_summary||{};const labels=Object.keys(days).sort();if(chart)chart.destroy();const canvas=$('dash-chart');if(canvas&&window.Chart)chart=new Chart(canvas,{type:'line',data:{labels,datasets:[{label:'סה״כ',data:labels.map(k=>days[k].total||0),tension:.3}]},options:{responsive:true,maintainAspectRatio:false}})}function init(){const to=$('dash-to'),from=$('dash-from'),button=$('dash-load');if(!to||!from)return;const t=new Date();to.value=iso(new Date(t));const s=new Date(t);s.setDate(s.getDate()-30);from.value=iso(s);button?.addEventListener('click',load);load()}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init()})();