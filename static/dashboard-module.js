(()=>{'use strict';let chart=null;const $=id=>document.getElementById(id);function iso(d){d.setMinutes(d.getMinutes()-d.getTimezoneOffset());return d.toISOString().slice(0,10)}function money(v){return Number(v||0).toLocaleString('he-IL',{minimumFractionDigits:2,maximumFractionDigits:2})}async function load(){const a=$('dash-from').value,b=$('dash-to').value;if(!a||!b)return;const r=await fetch(`/api/dashboard/summary?from=${encodeURIComponent(a)}&to=${encodeURIComponent(b)}`);if(!r.ok)return;const d=await r.json(),s=d.summary||{};$('dash-total').textContent=money(s.grand_total)+' ₪';$('dash-regular').textContent=money(s.regular_total)+' ₪';$('dash-extra').textContent=money(s.extra_total)+' ₪';$('dash-days').textContent=s.days_count||0;const products=d.product_summary||{};$('dash-products').innerHTML=Object.entries(products).sort((a,b)=>b[1].total-a[1].total).map(([name,x])=>`<tr><td><strong>${String(name)}</strong></td><td>${x.quantity||0}</td><td>${money(x.total)} ₪</td></tr>`).join('')||'<tr><td colspan="3" class="saas-empty">אין נתונים</td></tr>';const days=d.day_summary||{};const labels=Object.keys(days).sort();if(chart)chart.destroy();chart=new Chart($('dash-chart'),{type:'line',data:{labels,datasets:[{label:'סה״כ',data:labels.map(k=>days[k].total||0),tension:.3}]},options:{responsive:true,maintainAspectRatio:false}})}function init(){const t=new Date();$('dash-to').value=iso(new Date(t));const s=new Date(t);s.setDate(s.getDate()-30);$('dash-from').value=iso(s);$('dash-load').onclick=load;load()}init()})();